<div class="book">
  <%= tag.span("#{@book.name} #{@book.author}", class: "book-title") %>
  <%= link_to "Edit", edit_book_path(@book) %>
</div>

<div class="notes">
  <%= render @notes %>
  <%= will_paginate @notes %>
</div>


<%= render 'notes/form', book: @book, note: @note %>

<%= link_to 'Back', books_path %>
